#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int n,m;
    cout << "Enter the two digits: ";
    cin >> n >> m;
    cout << "Square root: " << sqrt(n) << endl;
    cout << "Cube root: "<< cbrt(m);
}